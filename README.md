# Notes

Given more time, I would have stream lined the sort by toggle functionality.

Moreover, I implemented some very basic tests but unfortunately was unable to find the datatest-ids on render so my test to count number of project cards fails.
